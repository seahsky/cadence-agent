# Local Claude Code settings — holds the GitHub fine-grained PAT, never commit
.claude/settings.local.json

# Worktrees Claude Code creates for parallel sessions
.claude/worktrees/

.env
.env.local

# Build output
dist/

node_modules/

# Anything cadence persists at runtime — sessions, memory, logs
data/

# Test and tooling caches
coverage/
*.tsbuildinfo
.vitest/
